# Paper Bots Pin Strategy Version

Paper Bots stay pinned to the Strategy Version selected when they are created or started. Editing a Strategy and creating a newer Strategy Version does not automatically change a running Paper Bot's behavior.

**Consequences**

Users must explicitly reconfigure or restart a Paper Bot to use a newer Strategy Version. This prevents live simulated execution from changing unexpectedly after Strategy edits.
