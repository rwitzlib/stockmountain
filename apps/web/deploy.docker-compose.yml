name: stockmountain-app
services:
  stockmountain-app:
    container_name: stockmountain-app
    image: ${IMAGE}
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    env_file:
      - ../${ENVIRONMENT}.env
