info:
  name: Runs
  type: http
  seq: 1

http:
  method: GET
  url: https://{{base_url}}{{port}}/api/market-data/runs
  params:
    - name: limit
      value: "5"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
