info:
  name: Get Backtest Universe (stub)
  type: http
  seq: 7

http:
  method: GET
  url: https://{{base_url}}{{port}}/api/backtest/universe/{{backtest_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Stub endpoint for unconstrained trade-universe exploration.
  Expected response: 501 Not Implemented until the exploration feature ships.
  Portfolio outcome remains at GET /api/backtest/result/{{backtest_id}}.
