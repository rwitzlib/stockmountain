info:
  name: Get Backtest Entry (latest)
  type: http
  seq: 2

http:
  method: GET
  url: https://{{base_url}}/api/backtest/{{backtest_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
