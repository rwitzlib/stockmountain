info:
  name: Read
  type: http
  seq: 3

http:
  method: GET
  url: https://{{base_url}}{{port}}/api/scanner/{{scanner_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
