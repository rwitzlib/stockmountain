info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: https://{{base_url}}{{port}}/api/scanner/{{scanner_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
