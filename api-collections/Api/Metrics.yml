info:
  name: Metrics
  type: http
  seq: 3

http:
  method: GET
  url: https://{{base_url}}{{port}}/api/metrics
  auth:
    type: basic
    username: "2652867"
    password: configured

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
