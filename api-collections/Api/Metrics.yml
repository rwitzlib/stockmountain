info:
  name: Metrics
  type: http
  seq: 3

http:
  method: GET
  url: https://{{base_url}}{{port}}/api/metrics
  auth:
    type: bearer
    token: asdf

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
