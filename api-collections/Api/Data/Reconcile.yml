info:
  name: Reconcile
  type: http
  seq: 1

http:
  method: POST
  url: https://{{base_url}}{{port}}/api/market-data/backfill
  body:
    type: json
    data: |-
      {
        "start": "2024-01-01",
        "end": "2024-01-31",
        "timespan": "minute",
        "multiplier": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
