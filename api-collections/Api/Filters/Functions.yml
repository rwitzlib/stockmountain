info:
  name: Functions
  type: http
  seq: 2

http:
  method: GET
  url: https://{{base_url}}{{port}}/api/filters/functions?context=chart
  params:
    - name: context
      value: chart
      type: query
      enabled: false
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
