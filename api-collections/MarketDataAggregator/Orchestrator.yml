info:
  name: Orchestrator
  type: http
  seq: 1

http:
  method: POST
  url: "{{protocol}}://{{base_url}}:10000/{{lambda_path}}"
  body:
    type: json
    data: |-
      {
        ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
